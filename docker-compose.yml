version: '3'

services:
  web:
    build: .
    ports:
      - "8080:8080"

  test:
    image: maven:3.9.6-eclipse-temurin-17
    volumes:
      - .:/app
    working_dir: /app
    command: ["./mvnw", "test"]

